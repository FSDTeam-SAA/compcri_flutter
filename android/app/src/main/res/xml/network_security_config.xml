<?xml version="1.0" encoding="utf-8"?>
<!--
    Cleartext HTTP is allowed only for development hosts and the Compcri
    staging API, which is served over plain HTTP on a bare IP.
    10.0.2.2 is the host machine as seen from the Android emulator.
    Everything else must use HTTPS.

    TODO: drop the 62.72.58.29 exception once the API is behind a domain
    with TLS; credentials and JWTs are sent in the clear over this host.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">62.72.58.29</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false"/>
</network-security-config>
